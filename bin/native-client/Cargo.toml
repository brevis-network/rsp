[package]
name = "native-client"
description = "Native Ethereum client executor for mac/x86"
version = "0.1.0"
edition = "2021"

[dependencies]
# workspace
rsp-client-executor = { path = "../../crates/executor/client" }

# misc
bincode = "1.3.3"
clap = { version = "4.5.7", features = ["derive", "env"] }
dotenv = "0.15.0"
tracing-subscriber = "0.3.18"
tracing.workspace = true
